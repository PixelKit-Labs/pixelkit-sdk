const fs=require('node:fs');const base='../delta-mobile/';
for(const file of ['package.json','package-lock.json','app.json']){const path=base+file;const data=JSON.parse(fs.readFileSync(path,'utf8'));if(file==='app.json'){if(data.expo.version!=='1.0.53')throw Error('Unexpected app version');data.expo.version='1.0.54';data.expo.android.versionCode=110;}else{if(data.version!=='1.0.53')throw Error('Unexpected package version');data.version='1.0.54';if(data.packages?.[''])data.packages[''].version='1.0.54';}fs.writeFileSync(path,JSON.stringify(data,null,2)+'\n');}
const path=base+'CHANGELOG.md';let log=fs.readFileSync(path,'utf8');log=log.replace('## [1.0.53]',`## [1.0.54] - 2026-09-19\n\n### Changed\n- Introduce the conversation-first shell, full-screen channels/session hub, compact provider controls, and Delta orb presentation.\n- Extract feature screens, conversation persistence, storage, domain models, shared design tokens and Help content into named responsibility folders with compatibility exports.\n- Preserve per-session drafts and originating-session submission ownership; present confirmations within the keyboard-avoiding shell.\n- Revise Settings, Connections, Agent and Activity presentation and distinguish unavailable platform capabilities from implemented behavior.\n- Add a dark web loading document and safe text-based runtime error reporting.\n- Keep sitemap/spec/PNG review references and implementation evidence explicit; visual parity and Android interaction acceptance remain in progress.\n\n### Verification\n- TypeScript and existing Node checks are recorded in the shared mobile UI implementation notes. Browser rendering was observed; full phone acceptance is not implied.\n\n## [1.0.53]`);fs.writeFileSync(path,log);
const p=base+'src/features/conversations/components/ComposerBar.tsx';let s=fs.readFileSync(p,'utf8');s=s.replace('Mic, Paperclip, Send, Square','Mic, Plus, Send, Square').replace('<Paperclip','<Plus').replace('size={18}\n            color={attachment','size={24}\n            color={attachment');fs.writeFileSync(p,s);
console.log('Updated mobile 1.0.54 / Android code110 and composer plus icon.');
